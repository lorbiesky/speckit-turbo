---
description: Classify a request and begin the appropriate Spec Kit Turbo workflow.
---

Interpret the user's request, inspect any active native workflow run, and route it to feature, bugfix, refactor, maintenance, hotfix, discovery, or constitution. If a material ambiguity, scope expansion, security concern, or human gate exists, stop and ask exactly for the needed decision. Preserve upstream `$speckit-*` commands. Use the matching `$speckit-turbo-*` command or installed `turbo-*` workflow; never create a parallel state file.

Report the selected workflow, current native phase, expected artifacts, and the next user action.
